package transport;

public class Bus extends transport {

    public Bus(String brand, String model, int year, String country, String color, int maxSped) {
        super(brand, model, year, country, color, maxSped);
    }
}
